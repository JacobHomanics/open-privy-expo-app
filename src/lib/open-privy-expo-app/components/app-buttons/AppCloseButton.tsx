import AppIoniconButton from "../buttons/ionicon-button/AppIoniconButton";

export default function AppCloseButton() {
    return <AppIoniconButton iconName="close-outline" accessibilityLabel="Close and go back to start" onPress={() => { }} />
}